/**
 * SECTION 6 — SP method
 * Phase 1: empty shell, correct ID, correct position in IA.
 * Content written in Phase 6.
 */
export default function MethodSection() {
  return (
    <section id="modszer" className="sp-section">
      <div className="sp-container">
        {/* TODO Phase 6: hangulat → ajánlat → bizalom → döntés
            + 3-job framework. Distinct from OfferSection. */}
      </div>
    </section>
  )
}
