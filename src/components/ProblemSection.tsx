/**
 * SECTION 2 — Problem
 * Phase 1: empty shell, correct ID, correct position in IA.
 * Content written in Phase 3.
 */
export default function ProblemSection() {
  return (
    <section id="problem" className="sp-section bg-sp-surface">
      <div className="sp-container">
        {/* TODO Phase 3: diagnose the real failure mode — not ugliness,
            but "slow-to-understand offer" causing lost inquiries. */}
      </div>
    </section>
  )
}
